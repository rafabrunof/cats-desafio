$(document).ready(function() {

    /* INPUT FORMULARIO*/
    $('#input').characterCounter();

    /* SCROLLSPY*/
    $('.scrollspy').scrollSpy();

    /* BUTTON FLOATING */
    $('.fixed-action-btn').floatingActionButton();

    /* SIDENAV */
    $('.sidenav').sidenav();

});
